const Title = () => {
  return (
    <div className="mb-12">
      <h1 className="mb-2 text-5xl font-medium">Bun + Elysia + React + Vite</h1>
      <p className="text-xl text-gray-500">Server-side rendering + Tailwind + ESLint + Prettier</p>
    </div>
  );
};

export default Title;
